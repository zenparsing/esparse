{
  "name": "esparse",
  "version": "0.2.0",
  "description": "An ECMAScript Parser",
  "homepage": "https://github.com/zenparsing/esparse",
  "devDependencies": {
    "esdown": "^1.1.21",
    "eslint": "^4.3.0"
  },
  "scripts": {
    "test": "eslint src/* && esdown test",
    "pack": "esdown - src/default.js dist/esparse.js -b -r -g esparse"
  }
}
