== TODO ==

- Module declarations cannot nest
- Create a list of early errors that we need to detect.
  - yield outside of generator
  - super outside of class
  - ?

== Known Issues ==

- u{ HexDigits } escapes where HexDigits evaluates to a
  code point requiring a surrogate pair in UTF-16 are not 
  supported.